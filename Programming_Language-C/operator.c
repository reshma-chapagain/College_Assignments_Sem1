//#include<stdio.h>

/*int main()
{
    int A, B;
    int sum, diff, prod, div, mod;
    A = 5;
    B = 10;
    sum = A + B;
    diff = B - A;
    prod = A * B;
    div = B / A;
    mod = B % A;
    printf("Sum: %d\n", sum);
    printf("Difference: %d\n", diff);
    printf("Product: %d\n", prod);
    printf("Division: %d\n", div);
    printf("Modulus: %d\n", mod);
   
    return 0;
}*/

/*#include<stdio.h>
int main()
{
    printf("Hello World\n");
    printf("Welcome to C Programming\n");
    return 0;
}

/* WAp to assign length and breadth, calculate area of rectangle and print it. Comment formula, add /n in printing*/

#include<stdio.h>
int main()
{   int length, breadth;
    int l = 10;
    int b = 5;
    int area;
    // Area of rectangle = length * breadth
    area = l * b; // Area formula
    printf("Area of rectangle: %d\n", area);
    return 0;
}